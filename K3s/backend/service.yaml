apiVersion: v1
kind: Service
metadata:
  name: ${PREFIX}volunti-backend
  namespace: doe25-group-13
  labels:
    app: ${PREFIX}volunti-backend
spec:
  type: ClusterIP
  selector:
    app: ${PREFIX}volunti-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
