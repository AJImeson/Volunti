# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: ${PREFIX}adminer
  namespace: doe25-group-13
spec:
  selector:
    app: ${PREFIX}adminer
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
---

