frontend-deploy:
  stage: deploy
  image: alpine:latest
  needs: ["backend-deploy"]
  variables:
    STACK_NAME: "volunti-$CI_PROJECT_NAME-$CI_COMMIT_REF_SLUG"
  script:
    - apk add --no-cache curl
    - sleep 10
    - curl -fk "http://${STACK_NAME}.doe25.swarm.chas-lab.dev" || exit 1
  environment:
    name: review/$CI_COMMIT_REF_SLUG
    url: https://${STACK_NAME}.doe25.swarm.chas-lab.dev
    on_stop: stop_review

stop_review:
  stage: cleanup
  script:
    - python delete.py
  environment:
    name: review/$CI_COMMIT_REF_SLUG
    action: stop
  when: manual
  allow_failure: true