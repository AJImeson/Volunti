frontend-deploy:
  stage: deploy
  image: alpine:latest
  needs: ["backend-deploy"]
  script:
    - apk add --no-cache curl
    - sleep 10
    - curl -f "http://doe25.swarm.chas-lab.dev:3000" || exit 1
