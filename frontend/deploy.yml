frontend-deploy:
  stage: deploy
  image: curlimages/curl:latest
  needs: ["backend-deploy"]
  script:
    - sleep 10
    - curl -f -k https://samba-$CI_PROJECT_NAME-$CI_COMMIT_REF_SLUG.doe25.swarm.chas-lab.dev || exit 1
