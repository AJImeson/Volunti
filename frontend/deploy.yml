frontend-deploy:
  stage: deploy
  image: alpine:latest
  needs: ["backend-deploy"]
  script:
    - apk add --no-cache curl
    - sleep 10
    - curl -f "http://portainer.doe25.swarm.chas-lab.dev:3090" || exit 1
