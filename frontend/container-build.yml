frontend-container-build:
  stage: container-build
  image: docker:latest
  services:

  variables:
    IMAGE_TAG:

  script:

    - echo "frontend-container"


  rules:
    - changes:
      - frontend/**/*
