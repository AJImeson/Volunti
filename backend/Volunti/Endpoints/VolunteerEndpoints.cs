using Microsoft.EntityFrameworkCore;

namespace Volunti.Endpoints
{
    public class VolunteerEndpoints
    {
        public static void RegisterEndpoints(WebApplication app)
        {
        }
    }
}
