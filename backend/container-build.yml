backend-container-build:
  stage: container-build
  image: docker:latest
  services:

  variables:
    IMAGE_TAG:

  script:
    - echo "backend-container"

    - rules:
      - changes:
        - backend/**/*
